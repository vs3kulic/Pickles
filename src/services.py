# -*- coding: utf-8 -*-

# ====================
# Task 4: Orders Class
# ====================
# Write an Orders class to help manage and process orders in your app:
# - Store a list of Order objects (or manage them however you like)
# [ ] Set up __init__ to initialize your orders collection
# [ ] add_order(order: Order) to add a new order
# [ ] get_order(order_id: str) to fetch an order by its ID (or return None if not found)
# [ ] list_orders() to return all orders as a list
# [ ] update_order(order_id: str, **kwargs) to update order details
# [ ] remove_order(order_id: str) to delete an order
# [ ] (Optional) Save or load orders to/from a file (serialization)
# [ ] Use getters and setters for orders if you want more control
# [ ] Static method: validate_order_data(data: dict) to check if order data is valid
# [ ] Class method: from_dict(data: dict) to create an Orders instance from a dictionary
# [ ] (Optional) Use an abstract base class if you want to share methods across services

